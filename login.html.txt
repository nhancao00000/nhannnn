<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng nhập | FoodDash</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;500;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
</head>
<body class="auth-page">
<main class="auth-shell">
    <section class="auth-panel auth-brand">
        <a class="brand back-link" href="index.html">FoodDash</a>
        <h1>Chào mừng bạn quay lại.</h1>
        <p>Đăng nhập để đặt món, xem giỏ hàng và tạo đơn hàng mới.</p>
    </section>
    <section class="auth-panel">
        <form id="loginForm" class="form-card">
            <h2>Đăng nhập</h2>
            <label>Email
                <input type="email" name="email" placeholder="ban@example.com" required>
            </label>
            <label>Mật khẩu
                <input type="password" name="password" placeholder="Nhập mật khẩu" required>
            </label>
            <button class="btn btn-primary btn-full" type="submit">Đăng nhập</button>
            <p id="loginMessage" class="form-message"></p>
            <p class="form-foot">Chưa có tài khoản? <a href="register.html">Đăng ký ngay</a></p>
        </form>
    </section>
</main>

<script>
document.getElementById("loginForm").addEventListener("submit", async (event) => {
    event.preventDefault();
    const form = event.target;
    const message = document.getElementById("loginMessage");
    message.textContent = "Đang xử lý...";

    const response = await fetch("php/login.php", {
        method: "POST",
        body: new FormData(form)
    });
    const data = await response.json();

    message.textContent = data.message;
    message.className = data.success ? "form-message success" : "form-message error";

    if (data.success) {
        localStorage.setItem("fooddash_user", JSON.stringify(data.user));
        window.location.href = data.user.role === "admin" ? "admin.html" : "menu.html";
    }
});
</script>
</body>
</html>
